server {
    listen 80;
    # listen 443 ssl;
    server_name pong.cdurdetrouver.fr;
    server_name www.pong.cdurdetrouver.fr;
    server_name ec2-35-180-227-136.eu-west-3.compute.amazonaws.com;
    server_name 35.180.227.136;

    root /var/www/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html =404;
    }

    location /static/js/ {
        add_header Content-Type application/javascript;
    }

    # SSL configuration (assuming you have SSL certificates)
    # ssl_certificate /etc/nginx/ssl/nginx.crt;
    # ssl_certificate_key /etc/nginx/ssl/nginx.key;
}
